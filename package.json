{
  "name": "wallified-vanilla",
  "version": "1.0.0",
  "description": "Vanilla JS E-commerce",
  "scripts": {
    "dev": "npx sirv-cli . --dev --host --port 3000"
  },
  "dependencies": {},
  "devDependencies": {
    "sirv-cli": "^2.0.2"
  }
}
